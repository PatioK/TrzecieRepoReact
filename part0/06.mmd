sequenceDiagram
    participant browser
    participant server

    Note right of browser: The user writes a new note and clicks "Save"

    Note right of browser: The JavaScript running in the browser creates the note object and immediately renders it locally (no reload)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa<br>with JSON data (e.g. {"content": "New note", "date": "2025-10-17"})
    activate server
    server-->>browser: {"message": "Note created"}
    deactivate server

    Note right of browser: The browser keeps showing the updated list of notes without reloading the page
